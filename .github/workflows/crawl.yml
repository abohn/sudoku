name: Crawl & Export

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:
    inputs:
      months:
        description: "Months of history to fetch (ignored when full_rebuild is true)"
        required: false
        default: "1"
      full_rebuild:
        description: "Re-crawl entire channel history (rebuilds DB from scratch)"
        type: boolean
        required: false
        default: false

permissions:
  contents: write
  issues: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      # The DB is gitignored; restore it from cache so incremental crawls work.
      # Split into restore + save so the updated DB is always written back,
      # even on an exact cache hit (the combined action skips saving on hits).
      - name: Restore database cache
        id: db-cache-restore
        uses: actions/cache/restore@v4
        with:
          path: data/sudoku.db
          key: sudoku-db-${{ runner.os }}-${{ github.run_id }}
          restore-keys: sudoku-db-${{ runner.os }}-

      - name: Run crawler
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          CTC_CHANNEL_ID: "@crackingthecryptic"
        run: |
          mkdir -p data
          if [ "${{ inputs.full_rebuild }}" = "true" ]; then
            python -m app.crawler.run --all
          else
            python -m app.crawler.run --months ${{ inputs.months || 1 }}
          fi

      - name: Export static data
        run: python scripts/export_static.py

      - name: Save database cache
        uses: actions/cache/save@v4
        with:
          path: data/sudoku.db
          key: sudoku-db-${{ runner.os }}-${{ github.run_id }}

      - name: Commit updated data.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add frontend/public/data.json
          git diff --staged --quiet && echo "Nothing to commit" && exit 0
          git commit -m "chore: update puzzle data $(date -u +%Y-%m-%d)"
          git push

      - name: Open issue on failure
        if: failure()
        run: |
          EXISTING=$(gh issue list --label "crawl-failure" --state open --json number --jq '.[0].number')
          if [ -n "$EXISTING" ]; then
            echo "Issue #$EXISTING already open, skipping."
          else
            BODY=$(printf "Crawl failed — data.json was not updated.\n\n**Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n\nClose this issue once the crawl is healthy again.")
            gh issue create \
              --title "Crawl failed: $(date -u +%Y-%m-%d)" \
              --body "$BODY" \
              --label "crawl-failure"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
